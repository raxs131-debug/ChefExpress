[build]

Comando de construcción para React

command = "npm run build"

Directorio donde se publican los archivos estáticos (el resultado de 'npm run build')

publish = "build"

[functions]

Directorio donde se encuentran todas las funciones serverless (Netlify Functions)

directory = "netlify/functions"

Tiempo de espera de 10 segundos para la función

timeout = 10

[[redirects]]

Regla de redirección para que todas las rutas internas sean manejadas por React (history API)

from = "/*"
to = "/index.html"
status = 200